# mwanachama-backend-actor (Go)

Open tasks only — 🚀 In Progress · 📋 Not Started · ⏸️ Blocked.
Everything else (completed rows, board context) is in [todo_done.md](todo_done.md).

| Task | Title | Status | Notes |
|------|-------|--------|-------|
| ACT2 | 🐞 **BUG — `DeleteGroupType` checks its worn-by-Groups guard against the GroupType's CURRENT `Name`, so renaming a GroupType silently defeats the guard and lets the type be deleted while a live Group still carries its old, now-orphaned name.** `grouptype_impl.go`'s `DeleteGroupType` counts `Group` rows `WHERE node_type = <current.Name>` — but `EditGroupType` deliberately never touches any `Group.NodeType` column when renaming (documented as intentional: `NodeType` is free text with no live foreign key). So immediately after a rename, the guard is comparing against a name no Group can possibly carry any more, always finds zero matches, and lets the delete through. Demonstrated directly against the manager (sqlite, in-process): created GroupType "Department" and a Group with `NodeType: "Department"`; confirmed the guard correctly refuses the delete pre-rename (`ErrGroupTypeWornByGroups`); called `EditGroupType(dept.ID, "Committee", ...)`; called `DeleteGroupType(dept.ID)` again — it now returns `nil` (success), `GetGroupType` correctly 404s afterward, but `GetGroup` on the original Group still returns `NodeType: "Department"`, a value that now names no existing GroupType and cannot be recovered through any lookup this package exposes. **Fix location**: `grouptype_impl.go`'s `DeleteGroupType` — track worn-ness by the GroupType's `ID` rather than its current `Name` (e.g. cascade the rename onto every matching `Group.NodeType` inside `EditGroupType`'s own update, or require the worn-check to compare against every name the type has ever held rather than only the current one). Pinned by `TestDeleteGroupType_PinsStaleWornCheckAfterRename` in `grouptype_stale_rename_test.go`, asserting current (broken) behavior — goes red once fixed, per the test's own comment. Found 2026-09-17 by the fleet integration-test sweep, in the newly-landed `GroupType` feature (commit `245e361`, moved here from `mwanachama-backend-agency`'s AGD-017 refactor the same day) — no prior sweep had seen this code. | 📋 | `go test ./...` and `go test -tags=integration ./...` both green with the new test added. |
| ACT1 | 🐞 **BUG — a raced duplicate `Attributes["phone"]` on `CreateActor` is misclassified as `ErrDuplicateID` and surfaces to an HTTP caller as `500 "internal error"` instead of the clean `400` a sequential duplicate gets.** `checkUniqueAttributes`'s Go-level precheck is documented as a pre-check only, backstopped by `gormstore.syncUniqueAttributeIndexes`'s DB-level partial unique index for the case where two concurrent `CreateActor` calls both race past the precheck — driven directly with a deterministic forced interleave (`db.Callback().Query().After("gorm:query")` barrier holding both goroutines' precheck reads until both have actually read back "zero matching rows", then releasing both to their own transaction/insert): the DB-level index *does* correctly stop the second insert (`UNIQUE constraint failed: index 'test_actors_attr_phone_uniq'`), so no duplicate row is ever written — but `actor_impl.go`'s `classifyDuplicateID` treats **any** unique-constraint violation on the actors table (Postgres SQLSTATE `23505`, or sqlite's `"UNIQUE constraint failed"` substring match) as a primary-key collision and returns `ErrDuplicateID`, even when the violated index is the phone-attribute index, not the row's `id`. Confirmed via the real `routes.ActorRoutes` mux + `httptest.Server` + `http.Client`: the losing concurrent `POST /actors` request gets `500 {"error":"internal error"}` — because `routes/actor.go`'s `actorStatusFor` has no `case` for `ErrDuplicateID` at all and falls through to its default 500 arm — where the *identical* duplicate-phone body sent sequentially (caught by the Go-level precheck, which correctly returns `ErrDuplicateAttribute`) gets a clean `400`. So the DB-level backstop the doc comments describe as existing specifically to catch this race does catch it, but produces a worse-quality error (wrong sentinel, opaque 500) than the case it exists to guard against would get without the backstop at all. **Fix location**: `actor_impl.go:57-70`'s `classifyDuplicateID` (distinguish the violated index/constraint name rather than treating every unique violation as a PK collision — e.g. match on the `test_actors_attr_phone_uniq`/`_attr_<name>_uniq` naming `gormstore.syncUniqueAttributeIndexes` gives each attribute index, returning `ErrDuplicateAttribute` for those and `ErrDuplicateID` only for the real primary-key constraint), and/or `routes/actor.go:18-28`'s `actorStatusFor` (map `ErrDuplicateID` to a real 4xx instead of leaving it to fall through to 500). Related, but not itself demonstrated as a live bug today (both `models.DefaultGroupProperties()` and `models.DefaultActorGroupAssignmentProperties()` are currently empty, so no `Unique` attribute exists to race on): `group_impl.go`'s `CreateGroup` calls no `classifyDuplicateID`-equivalent at all around its own insert, so a raced unique-attribute violation there would propagate as a fully raw, unclassified GORM/SQL error the moment either type ever gets a `Unique` property — worth a second look when that happens, not filed as its own row since it's unreachable today. Also noticed in the course of this probe, not filed as its own row: `gormstore/codesequence.go`'s `NextCode` (`SELECT` then conditional `Create`/`Update`, no row lock) has its own, unrelated concurrent-write race — two truly concurrent first-ever `NextCode` calls for the same entity type, or even two concurrent calls once the row exists, can produce sqlite `SQLITE_BUSY`/"database is locked" errors that propagate completely unclassified; on Postgres this would more likely serialize via row locking rather than error, so severity there is unconfirmed, but the missing `SELECT ... FOR UPDATE` (or equivalent) is real. | 📋 | Found 2026-09-17 by the fleet integration-test sweep, working the "Attributes uniqueness partial-index race" lead flagged in the prior sweep's report. Two pinning tests committed in `duplicate_attribute_race_test.go` (`TestCreateActor_PinsRacedDuplicatePhoneMisclassifiedAsID`, `TestActorRoutes_PinsRacedDuplicatePhoneReturns500NotConflict`) asserting the *current* broken classification/status code, using a deterministic forced-interleave technique (a single pooled sqlite connection + an `After("gorm:query")` callback barrier) rather than relying on timing luck — verified stable across 20 consecutive runs and clean under `go test -race`. Each test's own comment says what it must assert once ACT1 is fixed. `go test ./...`, `go test -tags=integration ./...`, and both again under `-race` all green with these added. |

